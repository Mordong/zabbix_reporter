# 🧪 TESTING.md — Zabbix Reporter v3.4

Подробное руководство по тестированию всего функционала приложения.  
Используется пользователями для проверки корректности работы и выявления ошибок.

---

## Содержание

- [Подготовка к тестированию](#подготовка-к-тестированию)
- [Как сообщать об ошибках](#как-сообщать-об-ошибках)
- [Чек-лист критичного функционала](#чек-лист-критичного-функционала-smoke-test)
- [Раздел 1. Установка и первый запуск](#раздел-1-установка-и-первый-запуск)
- [Раздел 2. Конфигурация и сохранение настроек](#раздел-2-конфигурация-и-сохранение-настроек)
- [Раздел 3. Подключение к Zabbix](#раздел-3-подключение-к-zabbix)
- [Раздел 4. Вкладка «🔴 Проблемы»](#раздел-4-вкладка--проблемы)
- [Раздел 5. Вкладка «🖥 Хосты»](#раздел-5-вкладка--хосты)
- [Раздел 6. Карточка хоста](#раздел-6-карточка-хоста)
- [Раздел 7. Вкладка «📋 События»](#раздел-7-вкладка--события)
- [Раздел 8. Вкладка «📈 Метрики»](#раздел-8-вкладка--метрики)
- [Раздел 9. Вкладка «🔐 Аутентификация»](#раздел-9-вкладка--аутентификация)
- [Раздел 10. Вкладка «⚖ Сравнение шаблонов»](#раздел-10-вкладка--сравнение-шаблонов)
- [Раздел 11. Вкладка «📤 Экспорт шаблонов»](#раздел-11-вкладка--экспорт-шаблонов)
- [Раздел 12. Вкладка «🔎 Дубликаты»](#раздел-12-вкладка--дубликаты)
- [Раздел 13. Экспорт отчётов (PDF/Excel/CSV)](#раздел-13-экспорт-отчётов-pdfexcelcsv)
- [Раздел 14. UI/UX тесты](#раздел-14-uiux-тесты)
- [Раздел 15. Сборка EXE](#раздел-15-сборка-exe)
- [Раздел 16. Стресс-тесты и граничные случаи](#раздел-16-стресс-тесты-и-граничные-случаи)
- [Раздел 17. Безопасность](#раздел-17-безопасность)
- [Таблица для отчёта о тестировании](#таблица-для-отчёта-о-тестировании)

---

## Подготовка к тестированию

### Что понадобится

| Компонент | Минимум | Желательно |
|-----------|---------|------------|
| Windows | 10 x64 | 11 x64 |
| Python | 3.8 | 3.10+ |
| Zabbix сервер | 6.0 | 7.x |
| Тестовые данные | 5 хостов, 10 проблем | 50+ хостов, разные типы интерфейсов, LDAP |

### Тестовое окружение Zabbix

Для полноценного тестирования на вашем Zabbix-сервере должно быть:

- ✅ Минимум **10 хостов** в разных группах
- ✅ Минимум **5 шаблонов** (в т.ч. похожих для тестирования сравнения)
- ✅ Хотя бы **один хост в обслуживании** (maintenance)
- ✅ Хотя бы **один отключённый хост**
- ✅ Интерфейсы **разных типов** — Agent, SNMP, IPMI или JMX
- ✅ Активные **проблемы** разной серьёзности
- ✅ Настроенный **LDAP user directory** (для раздела 9)
- ✅ Пользователь уровня **Super Admin** (для полного тестирования)
- ✅ Пользователь уровня **Zabbix User** (для тестирования прав)

### Подготовка данных для тестирования дубликатов

Создайте в Zabbix тестовые хосты для проверки поиска дубликатов:

| Hostname | IP | Тип интерфейса | Ожидаемый результат |
|----------|-----|----------------|---------------------|
| `test-srv-01` | 10.0.0.1 | Agent | Short vs FQDN (с test-srv-01.lab.local) |
| `test-srv-01.lab.local` | 10.0.0.2 | Agent | Short vs FQDN (с test-srv-01) |
| `Test-SRV-02` | 10.0.0.3 | Agent | Разный регистр (с test-srv-02) |
| `test-srv-02` | 10.0.0.3 | Agent | Разный регистр + одинаковый IP |
| `test-srv-03` | 10.0.0.4 | Agent | Одинаковый IP (с test-srv-04) |
| `test-srv-04` | 10.0.0.4 | Agent | Одинаковый IP (с test-srv-03) |

---

## Как сообщать об ошибках

При обнаружении проблемы соберите следующую информацию:

### Обязательно

1. **Версия приложения** — указана в заголовке окна (v3.4)
2. **Способ запуска** — `run.bat` / `python zabbix_reporter.py` / собранный `.exe`
3. **Версия Python** — `python --version`
4. **Версия Windows** — Settings → System → About
5. **Версия Zabbix** — отображается в шапке приложения после подключения
6. **Точный шаг** когда возникла ошибка (какая кнопка, что вводили)
7. **Ожидаемое поведение** vs **фактическое поведение**

### Желательно

- **Скриншот** окна с ошибкой
- **Содержимое окна «📋 Лог / Отладка»** (кнопка внизу окна) — скопируйте текст через кнопку «Сохранить»
- **Трейс из CMD** — если приложение запущено через `run.bat`, в окне CMD видны трейсы Python

### Шаблон отчёта об ошибке

```
Версия:        v3.4
Запуск:        python zabbix_reporter.py
Python:        3.11.5
Windows:       10 22H2
Zabbix:        7.0.5

Шаги для воспроизведения:
1. ...
2. ...
3. ...

Ожидаемое поведение:
...

Фактическое поведение:
...

Лог (если есть):
...
```

---

## Чек-лист критичного функционала (smoke test)

Базовая проверка что приложение не сломано. Должно занять 5–10 минут.

- [ ] Приложение запускается без ошибок
- [ ] Подключение к Zabbix успешно (кнопка «🔌 Подключиться»)
- [ ] Версия Zabbix отображается в шапке
- [ ] Кнопка «🔄 Обновить все данные» заполняет вкладки «Проблемы», «Хосты», «События»
- [ ] Двойной клик по хосту открывает карточку
- [ ] Экспорт в PDF работает — файл создаётся, открывается, кириллица читаема
- [ ] Настройки сохраняются в `config.json` после нажатия «💾 Сохранить настройки»
- [ ] После перезапуска приложения URL, Пользователь, Пароль восстанавливаются

Если хотя бы один пункт провален — тестирование остальных разделов бессмысленно, откройте issue.

---

## Раздел 1. Установка и первый запуск

### 1.1 Установка через run.bat

**Шаги:**
1. Распакуйте архив в любую папку
2. Дважды кликните `run.bat`

**Ожидаемо:**
- ✅ Откроется окно CMD
- ✅ pip устанавливает зависимости (openpyxl, reportlab, matplotlib)
- ✅ Приложение запускается, открывается основное окно
- ✅ В заголовке окна: `Zabbix Reporter v3.4`

**Возможные проблемы:**
- ❌ `'python' is not recognized` — Python не установлен или не в PATH
- ❌ `Permission denied` при установке pip — запустите cmd от администратора
- ❌ Ошибки сетевого подключения к PyPI — проверьте прокси/firewall

### 1.2 Ручная установка

```bat
pip install openpyxl reportlab matplotlib
python zabbix_reporter.py
```

**Ожидаемо:** то же что и через `run.bat`.

### 1.3 Запуск без зависимостей

Удалите `openpyxl`, `reportlab`, `matplotlib` и запустите:
```bat
pip uninstall openpyxl reportlab matplotlib -y
python zabbix_reporter.py
```

**Ожидаемо:**
- ✅ Приложение запускается
- ✅ В левой панели в разделе «💾 Экспорт» рядом с кнопками указано `PDF: pip install reportlab` и `Excel: pip install openpyxl`
- ✅ Кнопки экспорта отсутствующих форматов скрыты или неактивны
- ⚠️ Кнопка «📈 Построить график» не работает (без matplotlib)

---

## Раздел 2. Конфигурация и сохранение настроек

### 2.1 Первый запуск — config.json не существует

**Шаги:**
1. Удалите `config.json` из папки приложения (если есть)
2. Запустите приложение

**Ожидаемо:**
- ✅ Приложение запускается без ошибок
- ✅ Поля URL, Пользователь, Пароль пусты или содержат значения по умолчанию
- ✅ Не создаётся никаких ошибок

### 2.2 Сохранение настроек

**Шаги:**
1. Заполните URL, Пользователь, Пароль
2. Нажмите «💾 Сохранить настройки»

**Ожидаемо:**
- ✅ В статусной строке внизу: `Настройки сохранены: <полный путь>\config.json`
- ✅ В логе: `✓ Config saved: <путь>  (password encrypted)`
- ✅ Файл `config.json` создан рядом с `zabbix_reporter.py`
- ✅ Открыв `config.json` в блокноте, видно `"password": "enc:..."` (НЕ открытый текст)

### 2.3 Шифрование пароля

**Шаги:**
1. Сохраните настройки с паролем `TestPass123`
2. Откройте `config.json` в блокноте

**Ожидаемо:**
- ✅ Поле `password` имеет вид `"password": "enc:vGKxRCJkLm==..."`
- ✅ Оригинальный пароль `TestPass123` НЕ виден в файле
- ✅ После префикса `enc:` идёт base64-строка

### 2.4 Восстановление настроек при перезапуске

**Шаги:**
1. Сохраните настройки с заполненными полями
2. Закройте приложение
3. Запустите снова

**Ожидаемо:**
- ✅ URL, Пользователь заполнены из config.json
- ✅ Пароль отображается как точки/звёздочки
- ✅ При попытке подключения — подключение успешно (пароль расшифрован правильно)

### 2.5 Защита от копирования между ПК

**Шаги:**
1. На ПК #1 сохраните настройки
2. Скопируйте `config.json` на ПК #2
3. Запустите приложение на ПК #2

**Ожидаемо:**
- ✅ URL, Пользователь видны
- ✅ Поле Пароль пустое (расшифровка не удалась — другой MachineGuid)
- ✅ Приложение НЕ падает с ошибкой
- ⚠️ При попытке подключения ошибка логина — ожидаемое поведение

### 2.6 Миграция с legacy-конфига

**Шаги:**
1. Создайте `config.json` вручную:
```json
{
  "url": "https://zabbix.example.com",
  "user": "Admin",
  "password": "plain_text_password",
  "verify_ssl": true
}
```
2. Запустите приложение
3. В поле Пароль должно быть `plain_text_password`
4. Нажмите «Сохранить настройки»
5. Откройте `config.json` снова

**Ожидаемо:**
- ✅ Приложение НЕ падает при чтении plaintext-пароля
- ✅ Пароль показывается в поле как введённый
- ✅ После сохранения пароль в файле имеет вид `enc:...`

### 2.7 Сохранение других настроек

**Шаги:**
1. Измените «Мин. серьёзность» на «Warning»
2. Измените лимит на 1000
3. Измените даты «С» / «По»
4. В диалоге экспорта PDF снимите несколько галочек
5. Сохраните настройки
6. Перезапустите приложение

**Ожидаемо:**
- ✅ Все изменённые поля восстановлены
- ✅ При следующем открытии диалога экспорта — сохранённый выбор разделов

### 2.8 Сохранение при закрытии окна

**Шаги:**
1. Измените любое поле (не нажимая «Сохранить»)
2. Закройте приложение крестиком

**Ожидаемо:**
- ✅ Настройки автоматически сохраняются при закрытии

---

## Раздел 3. Подключение к Zabbix

### 3.1 Успешное подключение

**Шаги:**
1. Введите корректные URL, Пользователь, Пароль
2. Нажмите «🔌 Подключиться»

**Ожидаемо:**
- ✅ В правом верхнем углу: `● Подключено` (зелёный)
- ✅ В шапке отображается версия Zabbix: `Zabbix 7.x.x`
- ✅ Кнопка «🔌 Подключиться» становится неактивной (`☑ Подключено`)
- ✅ Кнопка «⏏ Отключиться» становится активной
- ✅ В логе: успешный ответ API

### 3.2 Неправильный пароль

**Шаги:**
1. Введите неверный пароль
2. Нажмите «Подключиться»

**Ожидаемо:**
- ❌ Диалог с ошибкой авторизации (`Login name or password is incorrect` или подобное)
- ✅ Приложение НЕ падает, предлагает ввести заново

### 3.3 Недоступный сервер

**Шаги:**
1. Введите URL несуществующего сервера: `https://invalid.example.com`
2. Попытайтесь подключиться

**Ожидаемо:**
- ❌ Ошибка сети (timeout, DNS, connection refused)
- ✅ Приложение обрабатывает ошибку корректно, не падает

### 3.4 Самоподписанный сертификат

**Шаги:**
1. Установите URL Zabbix с самоподписанным сертификатом (`https://...`)
2. Галочка «Проверять SSL» включена
3. Попытайтесь подключиться

**Ожидаемо:**
- ❌ Ошибка SSL certificate verify failed

**Шаги:**
4. Снимите галочку «Проверять SSL»
5. Попытайтесь подключиться

**Ожидаемо:**
- ✅ Подключение успешно

### 3.5 HTTP вместо HTTPS

**Шаги:**
1. Введите `http://zabbix.example.com` (без S)
2. Подключитесь

**Ожидаемо:**
- ✅ Подключение работает
- ⚠️ Через HTTP пароль передаётся в открытом виде

### 3.6 URL с и без /api_jsonrpc.php

Проверьте что все варианты URL работают:
- ✅ `https://zabbix.example.com`
- ✅ `https://zabbix.example.com/`
- ✅ `https://zabbix.example.com/zabbix` (если установлен в подпапке)

### 3.7 Отключение

**Шаги:**
1. Подключитесь
2. Нажмите «⏏ Отключиться»

**Ожидаемо:**
- ✅ Статус меняется на `○ Отключено`
- ✅ Все вкладки очищаются или становятся неактивны
- ✅ Кнопки Подключить/Отключить меняют состояние

### 3.8 Диагностика API

**Шаги:**
1. Подключитесь
2. Нажмите «🩺 Диагностика API»

**Ожидаемо:**
- ✅ В логе появляется отчёт: количество хостов, групп, прав пользователя
- ✅ Если прав не хватает для чего-то — сообщение указывает на это

---

## Раздел 4. Вкладка «🔴 Проблемы»

### 4.1 Загрузка проблем

**Шаги:**
1. Подключитесь
2. В левой панели задайте Мин. серьёзность = `0 – Not classified`
3. Нажмите «🔄 Обновить все данные»

**Ожидаемо:**
- ✅ Вкладка заполнена списком активных проблем
- ✅ Цветовая индикация по серьёзности (фиолетовый → синий → жёлтый → оранжевый → красный → бордовый)

### 4.2 Фильтр по серьёзности

**Шаги:**
1. Смените Мин. серьёзность на «4 – High»
2. Нажмите «Обновить»

**Ожидаемо:**
- ✅ Показаны только проблемы уровня High и Disaster

### 4.3 Сортировка по столбцам

**Шаги:**
1. Кликните по заголовку каждого столбца

**Ожидаемо:**
- ✅ Сортировка в обе стороны (ascending / descending)
- ✅ Правильная сортировка по дате (Начало), по числам (Подтв.)

### 4.4 Лимит записей

**Шаги:**
1. Установите лимит = 10
2. Обновите данные

**Ожидаемо:**
- ✅ В таблице не более 10 строк
- ✅ Если проблем больше — это видно в логе

---

## Раздел 5. Вкладка «🖥 Хосты»

### 5.1 Базовая загрузка

**Шаги:**
1. Подключитесь
2. Нажмите «🔄 Обновить все данные»
3. Перейдите на вкладку «🖥 Хосты»

**Ожидаемо:**
- ✅ Таблица с 10 колонками: Хост, Имя, Группы, Шаблоны, Статус, Доступн., Интерфейсы, Agent ver., Тригг., Обслуж.
- ✅ Каждая строка содержит данные

### 5.2 Колонка «Интерфейсы»

**Ожидаемо для разных типов хостов:**
- Один Agent интерфейс: `Agent 192.168.1.10:10050 ●`
- Несколько интерфейсов: `Agent 10.0.0.1:10050 ●  SNMP 10.0.0.1:161`
- Без интерфейсов: `—`

Значок `●` указывает основной интерфейс.

### 5.3 Колонка «Доступн.»

**Ожидаемо:**
- 🟢 `Available` — агент на связи
- 🔴 `Unavailable` — недоступен
- 🟡 `Unknown` — данных нет

### 5.4 Обновить расширенно

**Шаги:**
1. Нажмите «🔄 Обновить расширенно»

**Ожидаемо:**
- ✅ Заполняются колонки «Шаблоны» и «Agent ver.»
- ✅ Для SNMP/IPMI/JMX хостов пересчитывается доступность по `latest data`
- ✅ Хосты, у которых есть свежие данные (< 15 мин), показываются как Available даже если interface.available = Unknown

### 5.5 Фильтр по группе

**Шаги:**
1. В тулбаре выберите группу в «Группа»
2. Проверьте результат

**Ожидаемо:**
- ✅ Показаны только хосты из выбранной группы
- ✅ Выбор «— все —» сбрасывает фильтр

### 5.6 Фильтр по шаблону

**Шаги:**
1. Выберите шаблон в «Шаблон»

**Ожидаемо:**
- ✅ Показаны только хосты использующие этот шаблон

### 5.7 Текстовый поиск

**Шаги:**
1. Введите в поле поиска часть имени хоста

**Ожидаемо:**
- ✅ Фильтр работает в реальном времени при вводе
- ✅ Ищет по всем колонкам
- ✅ Регистронезависимый

### 5.8 Хосты в обслуживании

**Шаги:**
1. Включите хост в maintenance в Zabbix
2. Обновите данные

**Ожидаемо:**
- ✅ Столбец «Обслуж.» содержит `🔧 <имя maintenance>`
- ✅ Строка подсвечена оранжевым цветом

### 5.9 Отключённые хосты

**Ожидаемо:**
- ✅ В столбце «Статус» = `Disabled`
- ✅ Строка показана серым (тусклым) цветом

---

## Раздел 6. Карточка хоста

### 6.1 Открытие карточки

**Шаги:**
1. Дважды кликните по любому хосту в таблице  
   **или** выберите хост и нажмите `Enter`

**Ожидаемо:**
- ✅ Открывается модальное окно `Карточка хоста — <host>`
- ✅ Окно размером около 760×640
- ✅ Заголовок содержит host и visible name

### 6.2 Секции карточки

**Ожидаемые секции:**
- ● **Основное** — Host name, Visible name, Enabled/Disabled (цветом), Monitored by, Description
- ● **Host groups** — все группы через точку
- ● **Templates** — все шаблоны
- ● **Interfaces** — каждый с типом, IP, портом, статусом (✓/✗/?)
- ● **Tags** — теги с значениями (если есть)
- ● **Host macros** — макросы с типом (Secret скрыт как `***`)

### 6.3 Agent version в карточке

**Шаги:**
1. Откройте карточку хоста с Agent-интерфейсом
2. Найдите раздел Interfaces

**Ожидаемо:**
- ✅ Под строкой Agent интерфейса строка `Zabbix agent ver   6.4.3` (или `(не получен)`)

### 6.4 Скрытие секретных макросов

**Шаги:**
1. В Zabbix создайте хост с макросом типа `Secret`
2. Откройте его карточку

**Ожидаемо:**
- ✅ Значение секретного макроса отображается как `***`

### 6.5 Закрытие карточки

**Шаги:**
1. Нажмите «Закрыть» или крестик окна

**Ожидаемо:**
- ✅ Окно закрывается
- ✅ Основное окно активно, таблица хостов не изменилась

---

## Раздел 7. Вкладка «📋 События»

### 7.1 Загрузка событий

**Шаги:**
1. Установите период: С = 7 дней назад, По = сегодня
2. Нажмите «🔄 Обновить все данные»
3. Перейдите на вкладку «📋 События»

**Ожидаемо:**
- ✅ Таблица 7 колонок: ID, Серьёзность, Событие, Хост, Время, Восст., Длит.
- ✅ События в диапазоне дат

### 7.2 Колонка «Восст.»

**Ожидаемо:**
- ✅ Для восстановленных событий — дата/время восстановления
- ✅ Для активных — `—`

### 7.3 Колонка «Длит.»

**Ожидаемо:**
- ✅ Для восстановленных — длительность от начала до восстановления (`1h 23m 45s`)
- ✅ Для активных — длительность от начала до текущего момента

### 7.4 Фильтр по серьёзности

**Шаги:**
1. Снимите галочку «Disaster»
2. Нажмите «🔍 Применить» (или фильтр применяется автоматически)

**Ожидаемо:**
- ✅ В статусной строке: `Показано: X из Y`
- ✅ Событий уровня Disaster нет в таблице

### 7.5 Фильтр по хосту

**Шаги:**
1. Выберите конкретный хост в комбобоксе
2. Примените

**Ожидаемо:**
- ✅ Только события выбранного хоста

### 7.6 Фильтр по времени

**Шаги:**
1. Введите в «С»: `2026-04-20 10:00`
2. Введите в «По»: `2026-04-22 15:30`
3. Примените

**Ожидаемо:**
- ✅ Только события в указанном диапазоне

**Некорректный формат:**
- ✅ Неверный формат → фильтр игнорируется (показаны все)

### 7.7 Сброс фильтров

**Шаги:**
1. Задайте фильтры
2. Нажмите «⟲ Сброс»

**Ожидаемо:**
- ✅ Все чекбоксы серьёзности включены
- ✅ Хост = «— все —»
- ✅ Поля «С» и «По» очищены
- ✅ В таблице все события

### 7.8 Поле хоста заполняется автоматически

**Ожидаемо:**
- ✅ После загрузки событий выпадающий список хостов содержит все уникальные имена хостов из событий
- ✅ Сортировка по алфавиту

---

## Раздел 8. Вкладка «📈 Метрики»

### 8.1 Выбор хоста

**Шаги:**
1. Выберите хост из выпадающего списка
2. Нажмите «🔍 Загрузить метрики»

**Ожидаемо:**
- ✅ В таблице слева появляются все метрики хоста
- ✅ Колонки: Name, Key, Type, Value type, Последнее значение

### 8.2 Поиск метрик

**Шаги:**
1. В поле поиска введите `cpu`
2. Нажмите «Загрузить»

**Ожидаемо:**
- ✅ Показаны только метрики с `cpu` в имени/ключе

### 8.3 Построение графика

**Шаги:**
1. Выберите одну метрику (тип Numeric)
2. Нажмите «📈 Построить график»

**Ожидаемо:**
- ✅ Справа появляется график matplotlib
- ✅ Ось X — время, ось Y — значения
- ✅ Панель инструментов matplotlib работает (zoom, pan, save)

### 8.4 График для нескольких метрик

**Шаги:**
1. Выделите 2–3 метрики (Ctrl+клик)
2. Постройте график

**Ожидаемо:**
- ✅ На одном графике несколько линий
- ✅ Легенда с именами метрик

### 8.5 График текстовой метрики

**Шаги:**
1. Выберите метрику типа Text
2. Попытайтесь построить график

**Ожидаемо:**
- ✅ Сообщение что текстовые метрики не строятся, или график не появляется
- ✅ Приложение НЕ падает

### 8.6 Пустой период

**Шаги:**
1. Установите период «С» и «По» так, чтобы не было данных
2. Постройте график

**Ожидаемо:**
- ✅ Пустой график или сообщение об отсутствии данных
- ✅ Приложение НЕ падает

---

## Раздел 9. Вкладка «🔐 Аутентификация»

### 9.1 Загрузка данных

**Шаги:**
1. Подключитесь как **Super Admin**
2. Перейдите на вкладку «🔐 Аутентификация»
3. Нажмите «🔄 Загрузить»

**Ожидаемо:**
- ✅ В статусной строке: `Загружено: X LDAP, Y SAML серверов`
- ✅ В правой части — глобальные настройки аутентификации

### 9.2 Права не Super Admin

**Шаги:**
1. Подключитесь как обычный **Zabbix User**
2. Попытайтесь загрузить аутентификацию

**Ожидаемо:**
- ❌ Ошибка прав в логе
- ✅ Приложение НЕ падает

### 9.3 Глобальные настройки

**Ожидаемо видимо:**
- Default authentication (Internal / LDAP / HTTP)
- Парольная политика (мин. длина, требования)
- HTTP настройки
- SAML настройки
- MFA статус

### 9.4 Карточка LDAP-сервера

**Шаги:**
1. Кликните по серверу в списке слева

**Ожидаемо секции:**
- ● Подключение к LDAP (Host, Port, Base DN, Bind DN, StartTLS)
- ● Поиск пользователей (Search attribute)
- ● JIT Provisioning (если включён):
  - Group configuration
  - User Group Mapping — каждая пара с ролью и группами
  - Media Type Mapping

### 9.5 Разрешение имён ролей/групп

**Ожидаемо:**
- ✅ Вместо `roleid: "3"` отображается имя роли
- ✅ Вместо `usrgrpid: "7"` — имя группы пользователей
- ✅ Вместо `mediatypeid: "1"` — имя типа медиа

### 9.6 Экспорт в файл

**Шаги:**
1. Нажмите «💾 Выгрузить в файл»
2. Выберите папку

**Ожидаемо:**
- ✅ Созданы два файла: `zabbix_auth_YYYYMMDD_HHMMSS.json` и `zabbix_auth_YYYYMMDD_HHMMSS.pdf`
- ✅ В диалоге: `Файлы сохранены в: <путь>`
- ✅ JSON открывается, содержит полные данные
- ✅ PDF открывается, кириллица читается

### 9.7 Экспорт без reportlab

**Шаги:**
1. `pip uninstall reportlab -y`
2. Запустите приложение
3. Попробуйте выгрузку

**Ожидаемо:**
- ✅ JSON создаётся
- ⚠️ PDF не создаётся, сообщение об отсутствии reportlab
- ✅ Приложение НЕ падает

---

## Раздел 10. Вкладка «⚖ Сравнение шаблонов»

### 10.1 Загрузка списка шаблонов

**Шаги:**
1. Нажмите «🔄 Загрузить шаблоны»

**Ожидаемо:**
- ✅ Оба выпадающих списка (Шаблон A, Шаблон B) заполнены
- ✅ Формат: `<имя шаблона> [<id>]`

### 10.2 Сравнение одинаковых шаблонов

**Шаги:**
1. Выберите один шаблон и в A, и в B
2. Нажмите «⚖ Сравнить»

**Ожидаемо:**
- ❌ Предупреждение: «Выберите разные шаблоны»

### 10.3 Сравнение без выбора

**Шаги:**
1. Не выбирая шаблоны, нажмите «Сравнить»

**Ожидаемо:**
- ❌ Предупреждение: «Выберите оба шаблона»

### 10.4 Успешное сравнение

**Шаги:**
1. Выберите два разных шаблона (желательно похожих)
2. Нажмите «Сравнить»

**Ожидаемо:**
- ✅ В статусе: `A: ... | B: ... | Отличий: N`
- ✅ Результат распределён по 5 подвкладкам
- ✅ В каждой подвкладке: Items / Triggers / Discovery Rules и их прототипы
- ✅ Цветовая индикация статусов работает

### 10.5 Статусы строк

**Ожидаемо:**
| Статус | Цвет | Когда |
|--------|------|-------|
| Только A | 🔴 красный | Элемент есть только в A |
| Только B | 🟢 зелёный | Элемент есть только в B |
| Различия | 🟡 жёлтый | Одинаковое имя, разные параметры |
| Похожие | 🟠 оранжевый | Нечёткое совпадение ≥ 72% |
| Совпадает | ⬜ серый | Полное совпадение |

### 10.6 Столбец «Различия»

**Ожидаемо:**
- ✅ Для «Различия» — конкретное поле с различающимися значениями
- ✅ Для «Похожие» — указано «Схожесть: N%»

### 10.7 Экспорт в PDF

**Шаги:**
1. После сравнения нажмите «📄 PDF»
2. Сохраните файл

**Ожидаемо:**
- ✅ PDF создан
- ✅ Альбомная ориентация A4
- ✅ Цветные таблицы, легенда, кириллица
- ✅ По разделу на страницу

### 10.8 Экспорт в CSV

**Шаги:**
1. Нажмите «📋 CSV»
2. Выберите папку

**Ожидаемо:**
- ✅ 5 файлов: `compare_Items_DATE.csv`, `compare_Triggers_DATE.csv`, и т.д.
- ✅ UTF-8 с BOM (открывается в Excel без проблем с кириллицей)

---

## Раздел 11. Вкладка «📤 Экспорт шаблонов»

### 11.1 Загрузка списка

**Шаги:**
1. Нажмите «🔄 Загрузить список»

**Ожидаемо:**
- ✅ В таблице все шаблоны с колонками: ✓, Имя шаблона, Группы, ID
- ✅ В статусе: `Загружено шаблонов: N`

### 11.2 Пустой результат

**Шаги:**
1. Если у пользователя нет прав на шаблоны

**Ожидаемо:**
- ⚠️ В статусе: `Шаблонов не найдено — проверьте права пользователя`

### 11.3 Отметка тиками

**Шаги:**
1. Кликните по строке шаблона

**Ожидаемо:**
- ✅ Символ меняется с ☐ на ☑
- ✅ Повторный клик — обратно
- ✅ В статусе: `Выбрано: N`

### 11.4 Выбрать всё / Снять всё

**Шаги:**
1. Нажмите «☑ Выбрать все»
2. Затем «☐ Снять все»

**Ожидаемо:**
- ✅ Все строки отмечаются / снимаются
- ✅ Счётчик обновляется
- ✅ Ошибок нет

### 11.5 Выбор формата

**Шаги:**
- Переключите между YAML / XML / JSON

**Ожидаемо:**
- ✅ Радиокнопки работают

### 11.6 Экспорт

**Шаги:**
1. Отметьте 3 шаблона
2. Формат = YAML
3. Нажмите «📤 Экспортировать выбранные»
4. Выберите папку

**Ожидаемо:**
- ✅ Прогресс в статусе: `Экспорт 1/3...`, `2/3...`, `3/3...`
- ✅ По окончании: `Экспортировано: 3/3`
- ✅ В папке 3 файла с именами шаблонов и расширением `.yaml`
- ✅ Файлы открываются в текстовом редакторе, содержат YAML-структуру

### 11.7 Имена файлов с спецсимволами

**Шаги:**
1. Экспортируйте шаблон с именем содержащим `:`, `/`, `*`

**Ожидаемо:**
- ✅ Спецсимволы заменены на `_`
- ✅ Файл создан

### 11.8 Частичная ошибка

**Шаги:**
1. Отметьте много шаблонов (50+)
2. Экспортируйте в папку без прав на запись

**Ожидаемо:**
- ❌ Ошибки в логе
- ⚠️ Диалог «Частичный успех» с количеством и списком ошибок

### 11.9 YAML на старой версии Zabbix

**Шаги:**
1. Подключитесь к Zabbix < 5.4
2. Попробуйте YAML-экспорт

**Ожидаемо:**
- ⚠️ Автоматический fallback на XML
- ✅ Файлы всё равно создаются

---

## Раздел 12. Вкладка «🔎 Дубликаты»

### 12.1 Поиск без загруженных хостов

**Шаги:**
1. Без нажатия «Обновить все данные» перейдите во вкладку
2. Нажмите «🔍 Найти дубликаты»

**Ожидаемо:**
- ⚠️ Сообщение: «Сначала загрузите хосты»

### 12.2 Поиск с данными

**Шаги:**
1. Загрузите хосты
2. Нажмите «🔍 Найти дубликаты»

**Ожидаемо:**
- ✅ В статусе: `Найдено групп дубликатов: N  (затронуто хостов: M)`

### 12.3 Обнаружение разного регистра

**Тестовые данные:** `Test-SRV-02` и `test-srv-02`

**Ожидаемо:**
- ✅ Группа «По hostname»
- ✅ Причина: `Совпадает short-name «test-srv-02» (разный регистр)`

### 12.4 Обнаружение Short/FQDN

**Тестовые данные:** `test-srv-01` и `test-srv-01.lab.local`

**Ожидаемо:**
- ✅ Группа «По hostname»
- ✅ Причина: `Совпадает short-name «test-srv-01» (short vs FQDN)`

### 12.5 Обнаружение одинакового IP

**Тестовые данные:** два хоста с IP `10.0.0.4` в Agent интерфейсе

**Ожидаемо:**
- ✅ Группа «По IP»
- ✅ Причина: `IP 10.0.0.4 в интерфейсе Agent`

### 12.6 IP 127.0.0.1

**Ожидаемо:**
- ✅ Два хоста с `127.0.0.1` (локальные агенты) **попадают** в дубликаты (в отличие от предыдущих версий)
- ⚠️ Это нормально если у вас Zabbix server + Zabbix proxy на одной машине

### 12.7 Пустые IP не считаются

**Тестовые данные:** два хоста с интерфейсами без IP (только DNS)

**Ожидаемо:**
- ✅ Дубликат по IP НЕ обнаруживается

### 12.8 Разные типы интерфейсов

**Тестовые данные:** один хост с `Agent 10.0.0.1`, другой с `SNMP 10.0.0.1`

**Ожидаемо:**
- ✅ Дубликат НЕ обнаруживается (разные типы интерфейсов)

### 12.9 Фильтры показа

**Шаги:**
1. Снимите галочку «По hostname»
2. Снимите «По IP»

**Ожидаемо:**
- ✅ В списке отображаются только группы соответствующего типа

### 12.10 Детализация группы

**Шаги:**
1. Кликните по группе дубликатов

**Ожидаемо:**
- ✅ Справа таблица со всеми хостами в группе
- ✅ Колонки: Host name, Visible name, Группы, IP / интерфейсы, Статус, Доступн.
- ✅ Внизу — предупреждающее сообщение `⚠ <причина>`

### 12.11 Экспорт PDF

**Шаги:**
1. Нажмите «📄 PDF»
2. Сохраните файл

**Ожидаемо:**
- ✅ PDF с цветными таблицами
- ✅ Сводная статистика на первой странице
- ✅ Каждый тип на отдельной странице

### 12.12 Экспорт CSV

**Шаги:**
1. Нажмите «📋 CSV»

**Ожидаемо:**
- ✅ Один файл со всеми группами
- ✅ Колонки: Тип, Причина, Host name, Visible name, Группы, IP, Статус, Доступн., Host ID

---

## Раздел 13. Экспорт отчётов (PDF/Excel/CSV)

### 13.1 Диалог выбора разделов

**Шаги:**
1. Нажмите «📄 Экспорт PDF» или «📊 Экспорт Excel»

**Ожидаемо:**
- ✅ Открывается диалог `Настройки отчёта — PDF/Excel`
- ✅ Размер окна достаточный для отображения всех кнопок без скролла
- ✅ Окно **изменяемого размера**
- ✅ 6 чекбоксов разделов

### 13.2 Валидация выбора

**Шаги:**
1. Снимите все галочки
2. Нажмите «✅ Создать отчёт»

**Ожидаемо:**
- ⚠️ Предупреждение: «Выберите хотя бы один раздел»

### 13.3 Сохранение выбора

**Шаги:**
1. Выберите 3 раздела из 6
2. Создайте отчёт
3. Закройте приложение
4. Запустите, снова откройте диалог

**Ожидаемо:**
- ✅ Выбранные 3 галочки восстановлены

### 13.4 PDF — кириллица

**Ожидаемо:**
- ✅ Все русские тексты читаемы (НЕ квадраты)
- ✅ Шрифт Arial/Calibri/Segoe UI используется

### 13.5 PDF — форматирование

**Ожидаемо:**
- ✅ Шапка с фирменным цветом
- ✅ Цветные тайлы с числами (проблемы/хосты/события)
- ✅ Таблицы помещаются на страницу (не обрезаются)
- ✅ Нумерация страниц в футере

### 13.6 PDF — колонтитулы

**Ожидаемо:**
- ✅ `Zabbix Reporter v3.4` в шапке
- ✅ Дата и время генерации
- ✅ Номер страницы внизу

### 13.7 Excel

**Ожидаемо:**
- ✅ Файл открывается в Excel 2016+
- ✅ Отдельный лист на каждый раздел
- ✅ Заголовки стилизованы
- ✅ Чередующиеся строки
- ✅ Диаграмма на листе «Проблемы» (распределение по серьёзности)

### 13.8 CSV

**Шаги:**
1. Нажмите «📋 Экспорт CSV»
2. Выберите папку

**Ожидаемо:**
- ✅ Отдельный CSV на каждый раздел
- ✅ UTF-8 с BOM
- ✅ Открывается в Excel с правильной кодировкой

### 13.9 Экспорт без данных

**Шаги:**
1. Без загрузки данных попытайтесь экспортировать

**Ожидаемо:**
- ⚠️ Предупреждение: «Загрузите данные»

---

## Раздел 14. UI/UX тесты

### 14.1 Изменение размера окна

**Шаги:**
1. Тяните правый/нижний край окна

**Ожидаемо:**
- ✅ Таблицы растягиваются
- ✅ Тулбары остаются вверху
- ✅ Минимальный размер соблюдается (1200×700)

### 14.2 Сворачиваемые секции в левой панели

**Шаги:**
1. Кликните по заголовку «Подключение»

**Ожидаемо:**
- ✅ Секция сворачивается, стрелка меняется ▼ → ▶
- ✅ Остальные секции не смещаются
- ✅ При развороте секция появляется **на своём месте**, не внизу

### 14.3 Коллапс всех секций

**Шаги:**
1. Сверните все 3 секции

**Ожидаемо:**
- ✅ Остаются только заголовки
- ✅ При развороте всё восстанавливается

### 14.4 Переключение вкладок

**Шаги:**
1. Кликните по каждой из 9 вкладок

**Ожидаемо:**
- ✅ Переключение мгновенное
- ✅ Текст вкладок виден полностью (не обрезается `Метр...`)
- ✅ Активная вкладка выделена

### 14.5 Combobox видимость

**Шаги:**
1. Кликните в Combobox, затем кликните вне него

**Ожидаемо:**
- ✅ Текст в Combobox виден **во всех состояниях** (фокус/без фокуса/readonly)

### 14.6 Темная тема

**Ожидаемо:**
- ✅ Везде тёмный фон
- ✅ Текст светлый, читаемый
- ✅ Контрастность достаточная

### 14.7 Окно лога

**Шаги:**
1. Нажмите «📋 Лог / Отладка» внизу окна

**Ожидаемо:**
- ✅ Открывается окно лога
- ✅ Показаны все API-запросы и ответы
- ✅ Можно сохранить в файл
- ✅ Можно очистить

### 14.8 Индикатор загрузки

**Шаги:**
1. Нажмите «Обновить все данные»

**Ожидаемо:**
- ✅ Прогрессбар в верхней части окна
- ✅ Кнопки блокируются на время загрузки
- ✅ Статусная строка обновляется

### 14.9 Многопоточность

**Ожидаемо:**
- ✅ Во время загрузки данных UI остаётся отзывчивым (можно передвинуть окно)
- ✅ Нет зависаний

### 14.10 Обработка разрыва соединения

**Шаги:**
1. Подключитесь
2. Отключите сеть
3. Нажмите «Обновить»

**Ожидаемо:**
- ❌ Ошибка сети в логе
- ✅ Приложение НЕ падает
- ✅ Статус обновляется

---

## Раздел 15. Сборка EXE

### 15.1 Сборка через build.bat

**Шаги:**
1. Дважды кликните `build.bat`

**Ожидаемо:**
- ✅ PyInstaller устанавливается
- ✅ Сборка занимает 1–3 минуты
- ✅ В папке `dist\` появляется `ZabbixReporter.exe` (~50–70 МБ)

### 15.2 Запуск EXE

**Шаги:**
1. Дважды кликните `ZabbixReporter.exe`

**Ожидаемо:**
- ✅ При первом запуске — задержка 5–10 секунд (распаковка)
- ✅ Открывается приложение
- ✅ В заголовке `Zabbix Reporter v3.4`

### 15.3 EXE на ПК без Python

**Шаги:**
1. Скопируйте `ZabbixReporter.exe` на чистый Windows-ПК без Python
2. Запустите

**Ожидаемо:**
- ✅ Работает без установки Python

### 15.4 config.json рядом с EXE

**Шаги:**
1. Запустите EXE
2. Сохраните настройки
3. Проверьте папку рядом с EXE

**Ожидаемо:**
- ✅ `config.json` создан **рядом с EXE**
- ✅ Не во временной папке `_MEIxxxx`
- ✅ Настройки сохраняются между запусками

### 15.5 Антивирус

**Возможные проблемы:**
- ⚠️ Антивирус может блокировать PyInstaller-бинарник
- ✅ Решение — добавить папку в исключения или подписать EXE

---

## Раздел 16. Стресс-тесты и граничные случаи

### 16.1 Большое количество хостов

**Шаги:**
1. Подключитесь к Zabbix с 500+ хостами
2. Нажмите «Обновить»

**Ожидаемо:**
- ✅ Данные загружаются (может занять 10-30 секунд)
- ✅ UI остаётся отзывчивым
- ✅ Таблица отображает все хосты

### 16.2 Большое количество проблем

**Шаги:**
1. Установите лимит = 5000
2. Обновите

**Ожидаемо:**
- ✅ Загрузка и отображение работают
- ✅ Скролл работает плавно

### 16.3 Быстрые повторные клики

**Шаги:**
1. Многократно нажмите «Обновить»
2. Переключайтесь между вкладками

**Ожидаемо:**
- ✅ Нет дубликатов в данных
- ✅ Нет исключений
- ✅ Блокировка повторного запуска пока идёт загрузка

### 16.4 Очень длинные имена

**Шаги:**
1. Создайте хост с именем длиной 100+ символов
2. Загрузите

**Ожидаемо:**
- ✅ Имя отображается с обрезкой или скроллом
- ✅ Приложение НЕ падает

### 16.5 Специальные символы

**Тестовые данные:**
- Хосты с именами содержащими `<>&"'`
- Теги с unicode emoji
- Макросы с пробелами и спецсимволами

**Ожидаемо:**
- ✅ Все символы корректно отображаются
- ✅ Нет SQL-инъекций, XSS (N/A для десктопа)
- ✅ PDF генерирует без ошибок

### 16.6 Пустые ответы API

**Шаги:**
1. Используйте пользователя без доступа к хостам
2. Обновите

**Ожидаемо:**
- ✅ Пустые таблицы
- ✅ Сообщения об отсутствии данных
- ✅ Приложение НЕ падает

### 16.7 Медленный сервер

**Шаги:**
1. Подключитесь к медленному Zabbix (или через VPN с задержкой)

**Ожидаемо:**
- ✅ Индикатор загрузки работает весь период
- ✅ Нет таймаута на короткий период

---

## Раздел 17. Безопасность

### 17.1 Пароль в памяти

**Шаги:**
1. Подключитесь
2. Снимите дамп процесса (`procdump` или аналог)

**Ожидаемо:**
- ⚠️ Пароль присутствует в памяти в открытом виде (ограничение Python)
- ✅ Это документированное ограничение

### 17.2 Пароль в файле

**Шаги:**
1. Сохраните настройки с паролем `AAAAA12345!`
2. Откройте `config.json` в блокноте
3. Найдите ли строку `AAAAA12345!`

**Ожидаемо:**
- ✅ Строки в открытом виде **НЕТ**
- ✅ Есть `"password": "enc:..."`

### 17.3 Нет отправки пароля третьим лицам

**Ожидаемо:**
- ✅ Приложение НЕ отправляет данные куда-либо кроме указанного Zabbix-сервера
- ✅ Нет телеметрии, аналитики, «домашнего звонка»

### 17.4 HTTPS с самоподписанным сертификатом

**Ожидаемо:**
- ✅ По умолчанию SSL проверяется
- ⚠️ Отключение SSL даёт warning в логе

### 17.5 Безопасность config.json

**Шаги:**
1. Проверьте права файла

**Ожидаемо:**
- ⚠️ По умолчанию файл наследует права папки (в Windows может быть читаем всеми)
- ✅ Рекомендация — установить приложение в папку пользователя

---

## Таблица для отчёта о тестировании

Используйте эту таблицу при проведении полного тестирования:

| Раздел | Пункт | Результат | Комментарий |
|--------|-------|-----------|-------------|
| 1. Установка | 1.1 Установка run.bat | ⬜ Pass / ❌ Fail | |
| 1. Установка | 1.2 Ручная установка | ⬜ | |
| 1. Установка | 1.3 Без зависимостей | ⬜ | |
| 2. Конфиг | 2.1 Первый запуск | ⬜ | |
| 2. Конфиг | 2.2 Сохранение | ⬜ | |
| 2. Конфиг | 2.3 Шифрование | ⬜ | |
| 2. Конфиг | 2.4 Восстановление | ⬜ | |
| 2. Конфиг | 2.5 Защита копирования | ⬜ | |
| 2. Конфиг | 2.6 Миграция | ⬜ | |
| 3. Подключение | 3.1 Успешное | ⬜ | |
| 3. Подключение | 3.2 Неверный пароль | ⬜ | |
| 3. Подключение | 3.4 SSL | ⬜ | |
| 4. Проблемы | 4.1 Загрузка | ⬜ | |
| 4. Проблемы | 4.2 Фильтр серьёзности | ⬜ | |
| 5. Хосты | 5.1 Загрузка | ⬜ | |
| 5. Хосты | 5.2 Интерфейсы | ⬜ | |
| 5. Хосты | 5.4 Обновить расширенно | ⬜ | |
| 5. Хосты | 5.5 Фильтр группы | ⬜ | |
| 5. Хосты | 5.6 Фильтр шаблона | ⬜ | |
| 5. Хосты | 5.7 Поиск | ⬜ | |
| 5. Хосты | 5.8 Maintenance | ⬜ | |
| 6. Карточка | 6.1 Открытие | ⬜ | |
| 6. Карточка | 6.2 Секции | ⬜ | |
| 6. Карточка | 6.3 Agent ver | ⬜ | |
| 7. События | 7.1 Загрузка | ⬜ | |
| 7. События | 7.2 Восстановление | ⬜ | |
| 7. События | 7.3 Длительность | ⬜ | |
| 7. События | 7.4–7.7 Фильтры | ⬜ | |
| 8. Метрики | 8.1–8.4 | ⬜ | |
| 9. Аутентификация | 9.1–9.7 | ⬜ | |
| 10. Сравнение | 10.1–10.8 | ⬜ | |
| 11. Экспорт шаблонов | 11.1–11.9 | ⬜ | |
| 12. Дубликаты | 12.1–12.12 | ⬜ | |
| 13. Экспорт отчётов | 13.1–13.9 | ⬜ | |
| 14. UI/UX | 14.1–14.10 | ⬜ | |
| 15. EXE | 15.1–15.5 | ⬜ | |
| 16. Стресс | 16.1–16.7 | ⬜ | |
| 17. Безопасность | 17.1–17.5 | ⬜ | |

**Обозначения:**
- ⬜ — не тестировалось
- ✅ — Pass (работает корректно)
- ❌ — Fail (есть проблема)
- ⚠️ — Partial (работает частично)
- N/A — не применимо в вашем окружении

---

## Приоритизация тестирования

### 🔴 Высокий приоритет (критично)

1. Подключение к Zabbix (раздел 3)
2. Сохранение и шифрование настроек (раздел 2)
3. Загрузка хостов и проблем (разделы 4, 5)
4. Экспорт PDF (раздел 13)
5. Сборка EXE (раздел 15)

### 🟡 Средний приоритет

1. Фильтры событий (раздел 7)
2. Сравнение шаблонов (раздел 10)
3. Экспорт шаблонов (раздел 11)
4. Дубликаты (раздел 12)

### 🟢 Низкий приоритет

1. Карточка хоста детально (раздел 6)
2. Метрики (раздел 8)
3. UI/UX нюансы (раздел 14)
4. Стресс-тесты (раздел 16)

---

## Контакты для сообщения об ошибках

При обнаружении багов, отправьте отчёт с заполненным шаблоном (см. [Как сообщать об ошибках](#как-сообщать-об-ошибках)).

**Спасибо за помощь в тестировании Zabbix Reporter! 🚀**
